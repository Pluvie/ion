#ifdef ION_INCLUDED
static inline
#endif

void* tensor_iterate (
    struct tensor* tensor,
    struct vector* positions,
    struct vector* current
)
/**
 * ### Description
 *
 * ### Return Value
 *
 */;
