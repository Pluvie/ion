#ifdef ION_INCLUDED
static inline
#endif

bool sci_notation_convert (
    byte* result,
    enum types type,
    struct sci_notation* number,
    struct failure* error
)
/**
 * ### Description
 *
 * ### Return Value
 *
 */;
