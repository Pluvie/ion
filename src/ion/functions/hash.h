/**
 * Predefined hash functions for ⚡️ION⚡️ base types. */

int hash<int> (
    int value
);

int hash<dec> (
    dec value
);

int hash<bool> (
    bool value
);

int hash<char> (
    char value
);

int hash<char*> (
    char* value
);

int hash<string> (
    string value
);
