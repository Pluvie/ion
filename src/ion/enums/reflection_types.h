/* Defines the `enum reflection_types` ⚡️ION⚡️ type. */
enum reflection_types {
  /* Primitive types. */
  INT         = 1,
  DEC         = 2,
  STR         = 3,
  BOOL        = 4,

  /* Composite types. */
  STRUCT      = 11,
  ARRAY       = 12,
  POINTER     = 13,
  SELF        = 14,
  CONTAINER   = 15
};
