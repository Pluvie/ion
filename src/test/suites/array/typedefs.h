/**
 * Declines the array on the `i32` element type. */
#define   array_declination i32
#include  "ion/modules/array/declination.h"

/**
 * Declines the array on the `string` element type. */
#define   array_declination string
#include  "ion/modules/array/declination.h"

/**
 * Declines the array on the `User` element type. */
#define   array_declination User
#include  "ion/modules/array/declination.h"
